{
  "name": "bv-boardgame",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run -w @bv/game-core build && npm run -w server build && npm run -w web build",
    "dev": "npm run -w @bv/game-core build && concurrently -n server,web -c magenta,green \"npm run -w server dev\" \"npm run -w web dev\"",
    "dev:server": "npm run -w server dev",
    "dev:web": "npm run -w web dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "typescript": "^5.6.3"
  },
  "packageManager": "npm@10"
}
