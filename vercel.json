{
  "installCommand": "npm install",
  "buildCommand": "npm run -w @bv/game-core build && npm run -w web build",
  "outputDirectory": "apps/web/dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }],
  "env": {
    "VITE_SITE_URL": "https://spela.bryggverket.se"
  }
}
